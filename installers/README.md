# Installer Repository

Place production build artifacts here:

- `CreditAnalyzer-Windows.exe`
- `CreditAnalyzer-Mac.dmg`

These are not committed to version control. Run `npm run usb:prepare`
to verify both files are present before starting a fulfillment session.
